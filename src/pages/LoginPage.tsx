import { Background, FormLogin, Nav } from "components";

export function LoginPage() {

    return (
        <>
            <Background>
                <Nav />
                <FormLogin />
            </Background>
        </>
    )
}